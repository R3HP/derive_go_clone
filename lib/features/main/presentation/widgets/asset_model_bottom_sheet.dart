import 'package:flutter/material.dart';

class AssetsModalBottomSheet extends StatelessWidget {
  const AssetsModalBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    throw UnimplementedError();
  }
}
